spring:
  application:
    name: ConfigServer
  cloud:    config:      server:        git:          uri: https://github.com/lasvistass/configurazione.git
          username: lasvistass
          password: Cascata00!

server:  port: 8082